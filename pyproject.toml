[project]
name = "veridoc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pypdf>=6.6.2",
]

[tool.uv.workspace]
members = [
    "backend",
]
